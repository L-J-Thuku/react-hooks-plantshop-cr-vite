import { render, screen } from '@testing-library/react';
import App from '../../App';

describe('AllPlants Component', () => {
  test('renders all plants correctly', () => {
    render(<App />);
    // Add your specific tests here
  });
});